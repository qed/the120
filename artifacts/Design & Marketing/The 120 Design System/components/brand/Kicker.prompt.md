Mono section kicker — opens sections and cards. Join segments with `·`.

```jsx
<Kicker>01 · THE NETWORK</Kicker>
<Kicker tone="blush">FIVE GROUPS · ONE NETWORK</Kicker>
```

Use `blush` on dark/blue surfaces, `muted` for secondary metadata labels.
