export interface TestimonialCardProps {
  quote: string;
  name: string;
  /** Mono attribution line, e.g. "2 HOUR LEARNING STUDENT". */
  role?: string;
}
export function TestimonialCard(props: TestimonialCardProps): JSX.Element;
