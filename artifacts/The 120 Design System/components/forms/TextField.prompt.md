Text input with optional label + mono hint. Border goes ink on focus.

```jsx
<TextField label="Full name" placeholder="Child's first name" />
<TextField flat placeholder="Email" hint="CASL — EXPRESS OPT-IN" />
```

Default suits the dashboard/CRM (white, 10px). `flat` matches the squared marketing lead form.
